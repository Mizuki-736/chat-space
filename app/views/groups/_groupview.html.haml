%div.groups
  - current_user.groups.each do |group|
    %div.group
      = link_to group_messarges_path(group) do
        .group__name
          = group.name
        .group__message
          メッセージはまだありません
